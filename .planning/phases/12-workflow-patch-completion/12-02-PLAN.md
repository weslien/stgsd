---
phase: 12-workflow-patch-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/gustav/.claude/get-shit-done/workflows/research-phase.md
  - /Users/gustav/.claude/agents/gsd-phase-researcher.md
autonomous: true
requirements: []

must_haves:
  truths:
    - research-phase.md no longer calls gsd-tools roadmap get-phase or gsd-tools init phase-op
    - research-phase.md checks existing research via stclaude init plan-phase JSON, not disk ls
    - research-phase.md spawns gsd-phase-researcher with stclaude-based context (phase data from JSON, not file paths)
    - gsd-phase-researcher.md no longer calls gsd-tools init phase-op in Step 1
    - gsd-phase-researcher.md Step 6 persists research via stclaude write-research --content, not Write tool to disk
    - gsd-phase-researcher.md Step 7 gsd-tools commit is removed (no commit for stclaude data)
    - gsd-phase-researcher.md reads context from INIT.existingContext.content, not cat disk file
  artifacts:
    - /Users/gustav/.claude/get-shit-done/workflows/research-phase.md
    - /Users/gustav/.claude/agents/gsd-phase-researcher.md
  key_links:
    - research-phase.md passes stclaude JSON data inline to gsd-phase-researcher prompt
    - gsd-phase-researcher write-research call uses --content flag (not --content-file)
---

<objective>
Patch research-phase.md workflow and gsd-phase-researcher.md agent to replace disk I/O and gsd-tools commands with stclaude CLI equivalents.

Purpose: research-phase.md (the orchestrator) still calls gsd-tools roadmap get-phase and gsd-tools init phase-op. The gsd-phase-researcher.md agent (the worker) still reads CONTEXT.md from disk and writes RESEARCH.md to disk. Both must be updated to use stclaude commands.

Output: Both files patched so research data flows through SpacetimeDB. The researcher receives context inline from JSON and persists research via stclaude write-research.
</objective>

<execution_context>
@/Users/gustav/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
CRITICAL constraint: `stclaude write-research` does NOT have `--content-file` option. It only supports `--content <text>`. This is different from `stclaude write-context` which does support `--content-file`.

For the researcher agent (Step 6), use this pattern to handle large research content:
```bash
# Write research content to temp file for reference, but pass via --content
# For large content, use shell command substitution or heredoc capture
RESEARCH_CONTENT=$(cat /tmp/research-content.md)
stclaude write-research \
  --phase "${PHASE}" \
  --domain "${DOMAIN}" \
  --confidence "${CONFIDENCE}" \
  --content "${RESEARCH_CONTENT}" \
  --json
```

Also note: plan-phase.md (already patched) instructs the researcher with `--content-file` in its research prompt — this is a known bug in plan-phase.md. Phase 12 scope is to patch the agent itself; the plan-phase.md discrepancy is pre-existing (do NOT modify plan-phase.md in this plan).

The canonical patching pattern is: gsd-planner.md (already patched) uses `stclaude init plan-phase` and reads from JSON.

<interfaces>
From stclaude CLI (verified):
- stclaude init plan-phase <phase> --json
  Returns: { phase{number,name,slug,goal}, existingResearch{hasResearch,content,domain,confidence}, existingContext{hasContext,content}, existingPlans[], requirements[], projectContext{coreValue,constraints,keyDecisions} }
- stclaude write-research --phase <N> --domain <d> --confidence <c> --content <text> --json
  NOTE: NO --content-file option exists. Must use --content.
- stclaude get-phase <N> --json
  Returns: { phase{number,name,slug,goal,status,successCriteria}, plans[], requirements[] }
- stclaude roadmap analyze --json
  Returns: { phases[], milestones[], requirements[] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch research-phase.md orchestrator</name>
  <files>/Users/gustav/.claude/get-shit-done/workflows/research-phase.md</files>
  <action>
Read the full research-phase.md file. Replace all legacy gsd-tools and .planning/ disk I/O patterns:

1. **Step 1 (Normalize and Validate Phase)**: Replace `PHASE_INFO=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap get-phase "${PHASE}")` with:
   ```bash
   INIT=$(stclaude init plan-phase "${PHASE}" --json)
   ```
   Extract `phase_found` from: if stclaude returns PHASE_NOT_FOUND → phase not found error. Otherwise, parse `INIT.phase` for number, name, goal.

2. **Step 2 (Check Existing Research)**: Replace `ls .planning/phases/${PHASE}-*/RESEARCH.md 2>/dev/null` disk check with using `INIT.existingResearch.hasResearch` from the JSON (already loaded in Step 1). If `hasResearch` is true → offer update/view/skip options (same UX as before). Show `INIT.existingResearch.content` for the "View" option.

3. **Step 3 (Gather Phase Context)**: Remove `INIT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init phase-op "${PHASE}")` — this init was already done in Step 1. The phase_dir, padded_phase, state_path, requirements_path, context_path file paths are no longer needed. The researcher will receive data inline.

4. **Step 4 (Spawn Researcher)**: Update the Task prompt to the researcher agent. Replace file path references with inline data from stclaude JSON:
   - Remove `<files_to_read>` block with context_path, requirements_path, state_path (these were disk paths)
   - Replace with `<additional_context>` block containing:
     ```
     Phase: {INIT.phase.number} - {INIT.phase.name}
     Goal: {INIT.phase.goal}
     Requirements: {INIT.requirements (JSON serialized)}
     Existing context: {INIT.existingContext.content or "None"}
     ```
   - Update `<output>` instruction in researcher prompt: replace `Write to: .planning/phases/${PHASE}-{slug}/${PHASE}-RESEARCH.md` with `Persist via: stclaude write-research --phase "${PHASE}" --domain "{domain}" --confidence "{confidence}" --content "${RESEARCH_CONTENT}" --json`

5. **Step 5 (Handle Return)**: Keep as-is — the return format is behavior-based, not file-path-based.

After patching, verify the workflow reads phase data from stclaude JSON, passes it inline to the researcher, and instructs the researcher to persist via stclaude (not disk).
  </action>
  <verify>
    <automated>grep -c "gsd-tools.cjs" /Users/gustav/.claude/get-shit-done/workflows/research-phase.md</automated>
  </verify>
  <done>research-phase.md contains 0 references to gsd-tools.cjs. Step 2 uses INIT.existingResearch.hasResearch instead of disk ls. Step 4 researcher prompt contains stclaude write-research instruction instead of disk write path.</done>
</task>

<task type="auto">
  <name>Task 2: Patch gsd-phase-researcher.md agent</name>
  <files>/Users/gustav/.claude/agents/gsd-phase-researcher.md</files>
  <action>
Read the full gsd-phase-researcher.md file. Patch the execution_flow section (Steps 1, 6, 7):

1. **Step 1 (Receive Scope and Load Context)**: Replace:
   ```bash
   INIT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init phase-op "${PHASE}")
   # Extract from init JSON: phase_dir, padded_phase, phase_number, commit_docs
   ```
   With:
   ```bash
   INIT=$(stclaude init plan-phase "${PHASE}" --json)
   # Extract from JSON: INIT.phase.number, commit_docs from INIT.projectContext
   # No phase_dir or padded_phase needed (no disk paths)
   ```

   Replace `cat .planning/config.json` for nyquist_validation check with: extract from `INIT.projectContext` or `stclaude get-state --json` (check if nyquist validation setting is present in state).

   Replace `cat "$phase_dir"/*-CONTEXT.md 2>/dev/null` with: use `INIT.existingContext.content` directly (already embedded in init JSON). If `INIT.existingContext.hasContext` is false, no context to load.

2. **Step 6 (Write RESEARCH.md)**: Replace the entire "ALWAYS use Write tool to persist to disk" instruction with:
   ```
   ALWAYS persist research via stclaude write-research — mandatory regardless of commit_docs setting.

   Collect research content as RESEARCH_CONTENT variable, then call:
   stclaude write-research \
     --phase "${PHASE}" \
     --domain "${DOMAIN}" \
     --confidence "${CONFIDENCE}" \
     --content "${RESEARCH_CONTENT}" \
     --json

   NOTE: --content-file is NOT supported for write-research. Use --content directly.
   For large content, write to a temp file first then use shell substitution:
   RESEARCH_CONTENT=$(cat /tmp/research-content.md)
   stclaude write-research --phase ... --content "${RESEARCH_CONTENT}" --json
   rm /tmp/research-content.md

   No disk RESEARCH.md file is created. Data is persisted to SpacetimeDB.
   ```

   Update `<output_format>` section: change "Location: `.planning/phases/XX-name/{phase_num}-RESEARCH.md`" to "Persisted via: `stclaude write-research`"

3. **Step 7 (Commit Research)**: Remove the entire step or replace with:
   ```
   ## Step 7: Research Persisted

   Research is persisted to SpacetimeDB via stclaude write-research in Step 6.
   No git commit is needed for stclaude-persisted data.
   ```

4. **structured_returns section**: Update the "File Created" line:
   - Replace: `` `$PHASE_DIR/$PADDED_PHASE-RESEARCH.md` ``
   - With: `Research persisted to SpacetimeDB via stclaude write-research`

Keep the output_format's RESEARCH.md structure template as documentation (it describes the content structure, not a disk file). Only change the "Location:" line and the write mechanism.
  </action>
  <verify>
    <automated>grep -c "gsd-tools.cjs" /Users/gustav/.claude/agents/gsd-phase-researcher.md</automated>
  </verify>
  <done>gsd-phase-researcher.md contains 0 references to gsd-tools.cjs. Step 1 uses stclaude init plan-phase and reads context from INIT.existingContext.content. Step 6 instructs using stclaude write-research --content (not --content-file, not Write tool). Step 7 commit is removed. structured_returns shows SpacetimeDB persistence instead of disk file path.</done>
</task>

</tasks>

<verification>
After completing both tasks:
1. Run: `grep -rn "gsd-tools.cjs" /Users/gustav/.claude/get-shit-done/workflows/research-phase.md /Users/gustav/.claude/agents/gsd-phase-researcher.md` — should return 0 matches
2. Run: `grep -n "\.planning/phases" /Users/gustav/.claude/get-shit-done/workflows/research-phase.md` — should return 0 matches
3. Run: `grep -n "write-research" /Users/gustav/.claude/agents/gsd-phase-researcher.md` — should show stclaude write-research command
4. Run: `grep -n "content-file" /Users/gustav/.claude/agents/gsd-phase-researcher.md` — should return 0 matches (write-research does not support --content-file)
5. Run: `grep -n "Write tool" /Users/gustav/.claude/agents/gsd-phase-researcher.md` — should NOT show "Write tool" in Step 6 context
</verification>

<success_criteria>
- research-phase.md orchestrator uses stclaude init plan-phase JSON for phase validation and existing research check
- research-phase.md passes context inline to researcher (not file paths)
- gsd-phase-researcher.md agent uses stclaude write-research --content (not --content-file, not Write tool)
- No disk RESEARCH.md file written by the researcher
- No git commit step for research data
- 0 gsd-tools.cjs references in both files
</success_criteria>

<output>
After completion, store the summary via: stclaude write-summary (executor handles this, not planner)
</output>
