---
phase: 12-workflow-patch-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/gustav/.claude/get-shit-done/workflows/execute-plan.md
  - /Users/gustav/.claude/get-shit-done/workflows/discuss-phase.md
autonomous: true
requirements: []

must_haves:
  truths:
    - execute-plan.md no longer references gsd-tools.cjs or .planning/ file paths for state/plan loading
    - discuss-phase.md no longer writes CONTEXT.md to disk via Write tool; uses stclaude write-context instead
    - discuss-phase.md no longer calls gsd-tools init phase-op; uses stclaude init plan-phase
    - discuss-phase.md scout_codebase step uses stclaude get-codebase-map instead of ls .planning/codebase/
    - discuss-phase.md no longer commits CONTEXT.md via gsd-tools commit
  artifacts:
    - /Users/gustav/.claude/get-shit-done/workflows/execute-plan.md
    - /Users/gustav/.claude/get-shit-done/workflows/discuss-phase.md
  key_links:
    - execute-plan.md init_context step calls stclaude init execute-phase
    - discuss-phase.md write_context step calls stclaude write-context --content-file
---

<objective>
Patch execute-plan.md and discuss-phase.md to replace .planning/ file I/O and gsd-tools commands with stclaude CLI equivalents.

Purpose: These two HIGH-priority workflows still reference legacy disk paths and gsd-tools.cjs commands. Patching them ensures the executor and discuss workflows read/write from SpacetimeDB instead of .planning/ files.

Output: Two patched workflow files with all .planning/ disk I/O and gsd-tools references replaced by stclaude commands.
</objective>

<execution_context>
@/Users/gustav/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
The canonical patched workflows are execute-phase.md and plan-phase.md. Use them as the patching template.

Key patching rules:
1. `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init execute-phase` → `stclaude init execute-phase "${PHASE}" --json`
2. `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init phase-op` → `stclaude init plan-phase "${PHASE}" --json`
3. `cat .planning/STATE.md` → `stclaude get-state --json`
4. `ls .planning/phases/XX-name/*-PLAN.md` → plan data from `stclaude init execute-phase JSON` (INIT.plans[])
5. `grep -n "type=\"checkpoint"` on disk file → parse from INIT.plans[].tasks[] in JSON
6. `node gsd-tools.cjs phases list` → not needed when using stclaude JSON
7. Write CONTEXT.md to disk + gsd-tools commit → `stclaude write-context --phase --content-file` (no commit)
8. `ls .planning/codebase/*.md` → `stclaude get-codebase-map --json`
9. `node gsd-tools.cjs state record-session` → `stclaude write-session` (if command exists) or remove
10. `node gsd-tools.cjs state advance-plan` → `stclaude advance-plan`
11. `node gsd-tools.cjs state update-progress` → `stclaude update-progress`
12. `node gsd-tools.cjs state record-metric` → `stclaude record-metric`

CRITICAL: stclaude write-context supports `--content-file <path>` (this is the ONE write command with content-file support).

For the discuss-phase.md write_context step: write CONTEXT.md content to a temp file first, then call `stclaude write-context --phase "${PHASE}" --content-file /tmp/context-content.md --json`, then delete temp file. No git commit needed.

<interfaces>
From stclaude CLI help (verified):
- stclaude init execute-phase <phase> --json
  Returns: { executor_model, verifier_model, commit_docs, parallelization, branching_strategy, branch_name, phase{number,name,slug,goal}, plans[{id,planNumber,type,wave,objective,autonomous,requirements,dependsOn,status,content,tasks[],hasSummary,mustHaves[]}], requirements[] }
- stclaude init plan-phase <phase> --json
  Returns: { phase{number,name,slug,goal}, existingResearch{hasResearch,content}, existingContext{hasContext,content}, existingPlans[], requirements[], projectContext{coreValue,constraints,keyDecisions} }
- stclaude get-state --json
  Returns: { project, state{currentPhase,currentPlan,...}, phases[], plans[] }
- stclaude write-context --phase <N> --content-file <path> --json
- stclaude advance-plan --json
- stclaude update-progress --json
- stclaude record-metric --json
- stclaude get-codebase-map --json
  Returns: { hasMap, content }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch execute-plan.md</name>
  <files>/Users/gustav/.claude/get-shit-done/workflows/execute-plan.md</files>
  <action>
Read the full execute-plan.md file. Identify and replace all legacy gsd-tools and .planning/ disk I/O patterns:

1. **init_context step**: Replace `INIT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init execute-phase "${PHASE}")` with `INIT=$(stclaude init execute-phase "${PHASE_ARG}" --json)`. Update the JSON fields extracted — remove `phase_dir`, `phase_number` disk paths; data is now embedded in INIT JSON. Remove the `.planning/ missing: error` check (stclaude handles PHASE_NOT_FOUND error).

2. **identify_plan step**: Replace `ls .planning/phases/XX-name/*-PLAN.md` and `ls .planning/phases/XX-name/*-SUMMARY.md` disk listing with: `incomplete_plans = INIT.plans.filter(p => p.status !== 'Complete' && p.status !== 'done')`. Plan content is now `plan.content` from the JSON.

3. **parse_segments step**: Replace `grep -n "type=\"checkpoint" .planning/phases/XX-name/{phase}-{plan}-PLAN.md` with: parse checkpoint tasks from `INIT.plans[planIndex].tasks.filter(t => t.type.startsWith('checkpoint'))`. The plan JSON already contains the task list.

4. **load_prompt step**: Replace `cat .planning/phases/XX-name/{phase}-{plan}-PLAN.md` with: `PLAN_CONTENT = INIT.plans[planIndex].content` (plan content is in the JSON from step 1).

5. **previous_phase_check step**: Replace `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" phases list --type summaries --raw` with `stclaude get-state --json` to check phase history.

6. **State update commands** (in summary/completion sections): Replace any remaining gsd-tools state commands:
   - `gsd-tools state advance-plan` → `stclaude advance-plan --json`
   - `gsd-tools state update-progress` → `stclaude update-progress --json`
   - `gsd-tools state record-metric` → `stclaude record-metric --json`

7. **init_agent_tracking step**: The .planning/agent-history.json and .planning/current-agent-id.txt disk files can remain (they're ephemeral tracking, not planning state). Keep as-is.

8. Remove the `required_reading` section references to STATE.md and config.json files.

After patching, ensure the flow still makes logical sense: plan content flows from stclaude JSON → executor receives it inline → no disk reads of PLAN.md needed.
  </action>
  <verify>
    <automated>grep -c "gsd-tools.cjs" /Users/gustav/.claude/get-shit-done/workflows/execute-plan.md</automated>
  </verify>
  <done>execute-plan.md contains 0 references to gsd-tools.cjs. All .planning/phases/ PLAN.md disk read references replaced with stclaude JSON access. required_reading no longer instructs reading STATE.md or config.json files.</done>
</task>

<task type="auto">
  <name>Task 2: Patch discuss-phase.md</name>
  <files>/Users/gustav/.claude/get-shit-done/workflows/discuss-phase.md</files>
  <action>
Read the full discuss-phase.md file. Identify and replace all legacy gsd-tools and .planning/ disk I/O patterns:

1. **initialize step**: Replace `INIT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init phase-op "${PHASE}")` with `INIT=$(stclaude init plan-phase "${PHASE}" --json)`. Update extracted JSON fields: `has_context` → `INIT.existingContext.hasContext`, `has_research` → `INIT.existingResearch.hasResearch`, `has_plans` → `INIT.existingPlans.length > 0`, `plan_count` → `INIT.existingPlans.length`. Remove disk-based `phase_dir`, `padded_phase`, `phase_slug` path variables (no longer needed for file writes). For `phase_found` check: if stclaude returns PHASE_NOT_FOUND error, handle as phase not found.

2. **check_existing step**: Replace `ls ${phase_dir}/*-CONTEXT.md 2>/dev/null` disk check with using `INIT.existingContext.hasContext` from step 1 (already computed). No file listing needed.

3. **scout_codebase step, Step 1**: Replace `ls .planning/codebase/*.md 2>/dev/null` with:
   ```bash
   CODEBASE_MAP=$(stclaude get-codebase-map --json)
   ```
   If `CODEBASE_MAP.hasMap` is true, use `CODEBASE_MAP.content` as the codebase map. Skip to Step 3 (internal analysis). If `hasMap` is false, fall through to Step 2 (targeted grep).

4. **write_context step**: Replace the entire file-write section:
   - Remove: `mkdir -p ".planning/phases/${padded_phase}-${phase_slug}"`
   - Remove: `${phase_dir}/${padded_phase}-CONTEXT.md` file path construction
   - Replace Write tool usage with: write the CONTEXT.md content to a temp file `/tmp/context-${PHASE}.md`, then call `stclaude write-context --phase "${PHASE}" --content-file /tmp/context-${PHASE}.md --json`, then `rm /tmp/context-${PHASE}.md`
   - Update the `confirm_creation` step: remove the file path display (`Created: .planning/phases/...`), replace with "Context persisted to SpacetimeDB for phase ${PHASE}"

5. **git_commit step**: Remove or replace with a comment: `# Context persisted to SpacetimeDB via stclaude write-context — no git commit needed`

6. **update_state step**: Replace `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" state record-session` with `stclaude write-session` if that command exists. If it doesn't exist, remove the update_state step entirely (SpacetimeDB tracks state automatically through write-context).

After patching, verify the flow: init loads context from stclaude JSON → existing context checked from JSON → scout uses stclaude get-codebase-map → write_context persists to SpacetimeDB → no git commit for context.
  </action>
  <verify>
    <automated>grep -c "gsd-tools.cjs" /Users/gustav/.claude/get-shit-done/workflows/discuss-phase.md</automated>
  </verify>
  <done>discuss-phase.md contains 0 references to gsd-tools.cjs. The write_context step uses stclaude write-context instead of Write tool + disk path. The scout_codebase step uses stclaude get-codebase-map. The git_commit step is removed or replaced with a comment about SpacetimeDB persistence.</done>
</task>

</tasks>

<verification>
After completing both tasks:
1. Run: `grep -rn "gsd-tools.cjs" /Users/gustav/.claude/get-shit-done/workflows/execute-plan.md /Users/gustav/.claude/get-shit-done/workflows/discuss-phase.md` — should return 0 matches
2. Run: `grep -n "\.planning/phases" /Users/gustav/.claude/get-shit-done/workflows/execute-plan.md` — should show only agent-tracking references (.planning/agent-history.json, .planning/current-agent-id.txt), not PLAN.md or SUMMARY.md disk reads
3. Run: `grep -n "write-context" /Users/gustav/.claude/get-shit-done/workflows/discuss-phase.md` — should show the stclaude write-context command
4. Run: `grep -n "get-codebase-map" /Users/gustav/.claude/get-shit-done/workflows/discuss-phase.md` — should show the stclaude get-codebase-map command
</verification>

<success_criteria>
- execute-plan.md reads plan content from stclaude init execute-phase JSON, not disk files
- execute-plan.md has 0 gsd-tools.cjs references for state/plan operations
- discuss-phase.md persists context via stclaude write-context --content-file (temp file pattern)
- discuss-phase.md uses stclaude get-codebase-map for codebase scout
- discuss-phase.md has 0 gsd-tools.cjs references
- No git commits for CONTEXT.md (data in SpacetimeDB)
</success_criteria>

<output>
After completion, store the summary via: stclaude write-summary (executor handles this, not planner)
</output>
