---
phase: 10-todo-debug-tracking
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - ~/.claude/get-shit-done/workflows/add-todo.md
  - ~/.claude/get-shit-done/workflows/check-todos.md
  - ~/.claude/get-shit-done/workflows/diagnose-issues.md
  - ~/.claude/get-shit-done/templates/debug-subagent-prompt.md
autonomous: true
requirements:
  - TODO-05
  - TODO-06
  - DBG-05

must_haves:
  truths:
    - "add-todo.md no longer reads from gsd-tools.cjs or writes .planning/todos/pending/*.md files; instead calls stclaude add-todo to persist the todo"
    - "check-todos.md no longer reads from gsd-tools.cjs or reads .planning/todos/pending/*.md files; instead calls stclaude list-todos and stclaude complete-todo"
    - "diagnose-issues.md spawn_agents step creates debug sessions via stclaude write-debug instead of creating .planning/debug/*.md files"
    - "debug-subagent-prompt.md instructs agents to use stclaude write-debug --session-id for updates instead of writing to .planning/debug/*.md"
    - "If stclaude is not set up (PROJECT_NOT_FOUND or NOT_CONFIGURED error), all patched workflows show clear error with setup instructions"
  artifacts:
    - path: "~/.claude/get-shit-done/workflows/add-todo.md"
      provides: "Patched add-todo workflow using stclaude"
      contains: "stclaude add-todo"
    - path: "~/.claude/get-shit-done/workflows/check-todos.md"
      provides: "Patched check-todos workflow using stclaude"
      contains: "stclaude list-todos"
    - path: "~/.claude/get-shit-done/workflows/diagnose-issues.md"
      provides: "Patched diagnose-issues workflow for DB-backed debug sessions"
      contains: "stclaude write-debug"
    - path: "~/.claude/get-shit-done/templates/debug-subagent-prompt.md"
      provides: "Patched debug subagent prompt for DB-backed updates"
      contains: "stclaude write-debug --session-id"
  key_links:
    - from: "add-todo.md init_context step"
      to: "stclaude list-todos --json"
      via: "Replaced gsd-tools.cjs init todos call; area list derived from VALID_AREAS constant or omitted"
    - from: "add-todo.md create_file step"
      to: "stclaude add-todo"
      via: "Replaces .planning/todos/pending/ file write with stclaude add-todo call"
    - from: "check-todos.md init_context step"
      to: "stclaude list-todos --json"
      via: "Replaces gsd-tools.cjs init todos with stclaude list-todos for pending list"
    - from: "check-todos.md execute_action (Work on it now)"
      to: "stclaude complete-todo <id>"
      via: "Replaces mv .planning/todos/pending/... .planning/todos/done/ with stclaude complete-todo"
    - from: "diagnose-issues.md spawn_agents step"
      to: "stclaude write-debug --bug"
      via: "Creates debug session in SpacetimeDB before spawning agent; passes session ID to agent"
---

<objective>
Patch three GSD workflow files and one template to swap file I/O for stclaude CLI calls. Hard cutover: no file-based fallback. Existing .planning/todos/ and .planning/debug/ files are left as-is; all new todos and debug sessions go to SpacetimeDB.

This plan depends on Plan 01 (the 6 CLI commands must exist before workflows can call them).

Output: 4 patched workflow/template files. No new code files.
</objective>

<execution_context>
@/Users/gustav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-todo-debug-tracking/10-CONTEXT.md
@.planning/phases/10-todo-debug-tracking/10-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch add-todo.md and check-todos.md to use stclaude</name>
  <files>~/.claude/get-shit-done/workflows/add-todo.md, ~/.claude/get-shit-done/workflows/check-todos.md</files>
  <action>
Read both workflow files completely first. Apply surgical patches — replace ONLY the data source steps; preserve all display/interaction logic unchanged.

**Strategy:** Replace file I/O steps with stclaude CLI calls. Leave all other steps (extract_content, infer_area, offer_actions, etc.) unchanged. The user-facing experience stays the same; only the data storage mechanism changes.

---

**Patch `~/.claude/get-shit-done/workflows/add-todo.md`:**

Replace the `init_context` step. Old content reads gsd-tools and creates directories. New content calls `stclaude list-todos` to get existing area context:

Replace:
```xml
<step name="init_context">
Load todo context:

```bash
INIT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init todos)
```

Extract from init JSON: `commit_docs`, `date`, `timestamp`, `todo_count`, `todos`, `pending_dir`, `todos_dir_exists`.

Ensure directories exist:
```bash
mkdir -p .planning/todos/pending .planning/todos/done
```

Note existing areas from the todos array for consistency in infer_area step.
</step>
```

With:
```xml
<step name="init_context">
Load existing todo areas for context:

```bash
TODOS=$(~/.claude/bin/stclaude list-todos --json 2>&1)
```

If stclaude returns error with `"NOT_CONFIGURED"` or `"PROJECT_NOT_FOUND"` in the output:
```
Error: stclaude is not configured for this project.
Run: stclaude seed --name "<project-name>" to initialize.
```
Exit.

Extract `data` array from JSON result. Note existing `area` values for consistency in infer_area step.
</step>
```

Replace the `check_duplicates` step. Old content uses grep on .planning/todos/pending/*.md files. New content skips duplicate detection (visible in list-todos instead):

Replace:
```xml
<step name="check_duplicates">
```bash
# Search for key words from title in existing todos
grep -l -i "[key words from title]" .planning/todos/pending/*.md 2>/dev/null
```

If potential duplicate found:
1. Read the existing todo
2. Compare scope

If overlapping, use AskUserQuestion:
- header: "Duplicate?"
- question: "Similar todo exists: [title]. What would you like to do?"
- options:
  - "Skip" — keep existing todo
  - "Replace" — update existing with new context
  - "Add anyway" — create as separate todo
</step>
```

With:
```xml
<step name="check_duplicates">
Visually scan the `data` array from init_context for titles similar to the new todo title.

If a very similar title is found, use AskUserQuestion:
- header: "Duplicate?"
- question: "Similar todo exists: [existing title]. What would you like to do?"
- options:
  - "Skip" — keep existing todo
  - "Add anyway" — create as separate todo

No file scanning needed — todos are visible in the list.
</step>
```

Replace the `create_file` step. Old content writes a markdown file. New content calls stclaude add-todo:

Replace:
```xml
<step name="create_file">
Use values from init context: `timestamp` and `date` are already available.

Generate slug for the title:
```bash
slug=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" generate-slug "$title" --raw)
```

Write to `.planning/todos/pending/${date}-${slug}.md`:

```markdown
---
created: [timestamp]
title: [title]
area: [area]
files:
  - [file:lines]
---

## Problem

[problem description - enough context for future Claude to understand weeks later]

## Solution

[approach hints or "TBD"]
```
</step>
```

With:
```xml
<step name="create_file">
Persist todo to SpacetimeDB via stclaude:

```bash
RESULT=$(~/.claude/bin/stclaude add-todo "[title]" \
  --area "[area]" \
  --problem "[problem]" \
  --solution-hints "[solution]" \
  --file-refs "[files]" \
  --json)
```

Extract from result: `data.id` (todo ID for reference), `data.title`, `data.area`.

If stclaude returns an error, show the error message and exit.
</step>
```

Replace the `update_state` step. Old content updates STATE.md with todo count. New step is simplified:

Replace:
```xml
<step name="update_state">
If `.planning/STATE.md` exists:

1. Use `todo_count` from init context (or re-run `init todos` if count changed)
2. Update "### Pending Todos" under "## Accumulated Context"
</step>
```

With:
```xml
<step name="update_state">
Todos are stored in SpacetimeDB — no STATE.md update needed for todo count.
STATE.md is not modified by this workflow.
</step>
```

Replace the `git_commit` step. Old content commits the todo file. New step is simplified:

Replace:
```xml
<step name="git_commit">
Commit the todo and any updated state:

```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs: capture todo - [title]" --files .planning/todos/pending/[filename] .planning/STATE.md
```

Tool respects `commit_docs` config and gitignore automatically.

Confirm: "Committed: docs: capture todo - [title]"
</step>
```

With:
```xml
<step name="git_commit">
No file commit needed — todo is persisted in SpacetimeDB.
Skip this step.
</step>
```

Update `confirm` step to reference todo ID instead of filename:

Replace:
```
Todo saved: .planning/todos/pending/[filename]
```

With:
```
Todo saved: ID [data.id]
```

Also update the success_criteria at the bottom:

Replace:
```
- [ ] Directory structure exists
- [ ] Todo file created with valid frontmatter
- [ ] Problem section has enough context for future Claude
- [ ] No duplicates (checked and resolved)
- [ ] Area consistent with existing todos
- [ ] STATE.md updated if exists
- [ ] Todo and state committed to git
```

With:
```
- [ ] stclaude add-todo called with title, area, problem, solution-hints, file-refs
- [ ] Todo persisted in SpacetimeDB (confirmed by data.id in response)
- [ ] Problem section has enough context for future Claude
- [ ] No duplicates (checked from existing todos list)
- [ ] Area is one of 9 valid values
```

---

**Patch `~/.claude/get-shit-done/workflows/check-todos.md`:**

Replace the `init_context` step. Old content reads gsd-tools. New content calls stclaude list-todos:

Replace:
```xml
<step name="init_context">
Load todo context:

```bash
INIT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init todos)
```

Extract from init JSON: `todo_count`, `todos`, `pending_dir`.

If `todo_count` is 0:
```
No pending todos.

Todos are captured during work sessions with /gsd:add-todo.

---

Would you like to:

1. Continue with current phase (/gsd:progress)
2. Add a todo now (/gsd:add-todo)
```

Exit.
</step>
```

With:
```xml
<step name="init_context">
Load pending todos from SpacetimeDB:

```bash
TODOS=$(~/.claude/bin/stclaude list-todos --json 2>&1)
```

If stclaude returns error with `"NOT_CONFIGURED"` or `"PROJECT_NOT_FOUND"` in the output:
```
Error: stclaude is not configured for this project.
Run: stclaude seed --name "<project-name>" to initialize.
```
Exit.

Extract `data` array from JSON result. Set `todo_count` = length of `data` array.

If `todo_count` is 0:
```
No pending todos.

Todos are captured during work sessions with /gsd:add-todo.

---

Would you like to:

1. Continue with current phase (/gsd:progress)
2. Add a todo now (/gsd:add-todo)
```

Exit.
</step>
```

Replace the `list_todos` step to use the data from stclaude instead of parsing files:

Replace:
```xml
<step name="list_todos">
Use the `todos` array from init context (already filtered by area if specified).

Parse and display as numbered list:
```

With:
```xml
<step name="list_todos">
Use the `data` array from init_context (filter by area if --area argument provided in parse_filter step).

Display as numbered list:
```

(Note: only the first sentence changes; the display format block below stays unchanged)

Replace the `load_context` step. Old content reads the .md file. New content uses the already-loaded row data:

Replace:
```xml
<step name="load_context">
Read the todo file completely. Display:

```
## [title]

**Area:** [area]
**Created:** [date] ([relative time] ago)
**Files:** [list or "None"]

### Problem
[problem section content]

### Solution
[solution section content]
```

If `files` field has entries, read and briefly summarize each.
</step>
```

With:
```xml
<step name="load_context">
Use the selected item from the `data` array (already in memory from init_context). Display:

```
## [title]

**Area:** [area]
**ID:** [id]
**Created:** [age] ago
**Files:** [fileRefs or "None"]

### Problem
[problem]

### Solution
[solutionHints or "TBD"]
```

If `fileRefs` has entries (comma-separated paths), briefly describe what each file is for if visible in context.
</step>
```

Replace the `execute_action` "Work on it now" sub-action. Old content moves .md file. New content calls stclaude complete-todo:

Replace:
```
**Work on it now:**
```bash
mv ".planning/todos/pending/[filename]" ".planning/todos/done/"
```
Update STATE.md todo count. Present problem/solution context. Begin work or ask how to proceed.
```

With:
```
**Work on it now:**
```bash
~/.claude/bin/stclaude complete-todo [id] --json
```
Confirm completion. Present problem/solution context from the todo. Begin work or ask how to proceed.
```

Replace the `update_state` step:

Replace:
```xml
<step name="update_state">
After any action that changes todo count:

Re-run `init todos` to get updated count, then update STATE.md "### Pending Todos" section if exists.
</step>
```

With:
```xml
<step name="update_state">
Todos are stored in SpacetimeDB — no STATE.md update needed for todo count.
Skip this step.
</step>
```

Replace the `git_commit` step:

Replace:
```xml
<step name="git_commit">
If todo was moved to done/, commit the change:

```bash
git rm --cached .planning/todos/pending/[filename] 2>/dev/null || true
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs: start work on todo - [title]" --files .planning/todos/done/[filename] .planning/STATE.md
```

Tool respects `commit_docs` config and gitignore automatically.

Confirm: "Committed: docs: start work on todo - [title]"
</step>
```

With:
```xml
<step name="git_commit">
No file commit needed — todo status is updated in SpacetimeDB.
Skip this step.
</step>
```

Update the success_criteria at the bottom:

Replace:
```
- [ ] All pending todos listed with title, area, age
- [ ] Area filter applied if specified
- [ ] Selected todo's full context loaded
- [ ] Roadmap context checked for phase match
- [ ] Appropriate actions offered
- [ ] Selected action executed
- [ ] STATE.md updated if todo count changed
- [ ] Changes committed to git (if todo moved to done/)
```

With:
```
- [ ] All pending todos listed with title, area, age (from stclaude list-todos)
- [ ] Area filter applied if specified
- [ ] Selected todo's full context displayed
- [ ] Roadmap context checked for phase match
- [ ] Appropriate actions offered
- [ ] Selected action executed (stclaude complete-todo if "Work on it now")
```
  </action>
  <verify>
    <automated>grep -c "stclaude add-todo" ~/.claude/get-shit-done/workflows/add-todo.md && grep -c "stclaude list-todos" ~/.claude/get-shit-done/workflows/check-todos.md && grep -c "stclaude complete-todo" ~/.claude/get-shit-done/workflows/check-todos.md</automated>
  </verify>
  <done>add-todo.md contains `stclaude add-todo` call in create_file step. check-todos.md contains `stclaude list-todos` in init_context and `stclaude complete-todo` in execute_action. Neither workflow references gsd-tools.cjs or .planning/todos/ file paths in the patched steps.</done>
</task>

<task type="auto">
  <name>Task 2: Patch diagnose-issues.md and debug-subagent-prompt.md for DB-backed debug sessions</name>
  <files>~/.claude/get-shit-done/workflows/diagnose-issues.md, ~/.claude/get-shit-done/templates/debug-subagent-prompt.md</files>
  <action>
Read both files completely first. Apply surgical patches to the specific steps that create/write .planning/debug/*.md files. Preserve all orchestration logic (parse_gaps, report_plan, collect_results, update_uat, report_results) unchanged except where file references appear.

**Context on current diagnose-issues.md structure:**
- `parse_gaps`: Reads UAT.md gaps — NO file I/O to debug dir; leave unchanged
- `report_plan`: Prints plan — NO file I/O; leave unchanged
- `spawn_agents`: Creates DEBUG-{slug}.md AND spawns agent — PATCH the file creation part
- `collect_results`: Collects agent results mentioning debug file paths — PATCH the result format
- `update_uat`: Updates UAT.md with `debug_session` field — PATCH field value (ID not path)
- `report_results`: Reports to user — PATCH "Debug sessions:" line
- `<paths>` block at top: references DEBUG_DIR — REMOVE or note it's no longer used

---

**Patch `~/.claude/get-shit-done/workflows/diagnose-issues.md`:**

Replace the `<paths>` block at the top:

Replace:
```xml
<paths>
DEBUG_DIR=.planning/debug

Debug files use the `.planning/debug/` path (hidden directory with leading dot).
</paths>
```

With:
```xml
<paths>
Debug sessions are persisted in SpacetimeDB via stclaude write-debug.
No local .planning/debug/ directory is used.
</paths>
```

Replace the `spawn_agents` step to create DB-backed debug sessions before spawning agents:

Replace:
```xml
<step name="spawn_agents">
**Spawn debug agents in parallel:**

For each gap, fill the debug-subagent-prompt template and spawn:

```
Task(
  prompt=filled_debug_subagent_prompt + "\n\n<files_to_read>\n- {phase_dir}/{phase_num}-UAT.md\n- .planning/STATE.md\n</files_to_read>",
  subagent_type="general-purpose",
  description="Debug: {truth_short}"
)
```

**All agents spawn in single message** (parallel execution).

Template placeholders:
- `{truth}`: The expected behavior that failed
- `{expected}`: From UAT test
- `{actual}`: Verbatim user description from reason field
- `{errors}`: Any error messages from UAT (or "None reported")
- `{reproduction}`: "Test {test_num} in UAT"
- `{timeline}`: "Discovered during UAT"
- `{goal}`: `find_root_cause_only` (UAT flow - plan-phase --gaps handles fixes)
- `{slug}`: Generated from truth
</step>
```

With:
```xml
<step name="spawn_agents">
**Create debug sessions and spawn agents in parallel:**

For each gap:

1. Create a debug session in SpacetimeDB:
```bash
SESSION=$(~/.claude/bin/stclaude write-debug \
  --bug "[truth]" \
  --timeline "Discovered during UAT, Test [test_num]" \
  --json)
SESSION_ID=$(echo $SESSION | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['id'])")
```

2. Fill the debug-subagent-prompt template with `{session_id}` = SESSION_ID and spawn:

```
Task(
  prompt=filled_debug_subagent_prompt + "\n\n<files_to_read>\n- {phase_dir}/{phase_num}-UAT.md\n- .planning/STATE.md\n</files_to_read>",
  subagent_type="general-purpose",
  description="Debug: {truth_short}"
)
```

**All session creations and agent spawns happen in a single message** (parallel execution).

Template placeholders:
- `{truth}`: The expected behavior that failed
- `{expected}`: From UAT test
- `{actual}`: Verbatim user description from reason field
- `{errors}`: Any error messages from UAT (or "None reported")
- `{reproduction}`: "Test {test_num} in UAT"
- `{timeline}`: "Discovered during UAT"
- `{goal}`: `find_root_cause_only` (UAT flow - plan-phase --gaps handles fixes)
- `{session_id}`: SpacetimeDB debug session ID (NEW — replaces {slug} file path)
</step>
```

Replace the collect_results agent return format reference from file path to session ID:

In the `collect_results` step, replace:
```
**Debug Session:** ${DEBUG_DIR}/{slug}.md
```

With:
```
**Debug Session ID:** {session_id}
```

In the `collect_results` step, replace:
```
  debug_path: Path to debug session file
```

With:
```
  session_id: SpacetimeDB debug session ID
```

In the `update_uat` step, replace:
```
  debug_session: .planning/debug/comment-not-refreshing.md
```

With:
```
  debug_session_id: {session_id}
```

In the `report_results` step, replace:
```
Debug sessions: ${DEBUG_DIR}/
```

With:
```
Debug sessions: SpacetimeDB (use `stclaude get-debug --session-id {id}` to retrieve)
```

In the failure_handling section, replace:
```
**Agent times out:**
- Check DEBUG-{slug}.md for partial progress
- Can resume with /gsd:debug
```

With:
```
**Agent times out:**
- Check SpacetimeDB for partial progress: `stclaude get-debug --session-id {session_id}`
- Can resume with /gsd:debug
```

---

**Patch `~/.claude/get-shit-done/templates/debug-subagent-prompt.md`:**

The template currently instructs agents to create `.planning/debug/{slug}.md`. Patch to use stclaude write-debug --session-id for updates.

In the Template section, replace:
```
<debug_file>
Create: .planning/debug/{slug}.md
</debug_file>
```

With:
```
<debug_session>
Session ID: {session_id}
Update via: stclaude write-debug --session-id {session_id} --bug "{truth}" --hypotheses "..." --checkpoints "..." --timeline "..."
</debug_session>
```

In the Placeholders table, replace the `{slug}` row:
```
| `{slug}` | Generated | `auth-screen-dark` |
```

With:
```
| `{session_id}` | From stclaude write-debug (created by orchestrator) | `42` |
```

In the Continuation section, replace:
```
<prior_state>
Debug file: @.planning/debug/{slug}.md</prior_state>
```

With:
```
<prior_state>
Debug session ID: {session_id}
Retrieve via: stclaude get-debug --session-id {session_id}</prior_state>
```

In the Usage → From /gsd:debug section, keep the existing reference but note:
The `/gsd:debug` workflow spawns the agent directly — session creation happens in the debug workflow, not here.
  </action>
  <verify>
    <automated>grep -c "stclaude write-debug" ~/.claude/get-shit-done/workflows/diagnose-issues.md && grep -c "stclaude write-debug --session-id" ~/.claude/get-shit-done/templates/debug-subagent-prompt.md && ! grep -q "planning/debug/" ~/.claude/get-shit-done/workflows/diagnose-issues.md && echo "No .planning/debug/ references remain in diagnose-issues.md"</automated>
  </verify>
  <done>diagnose-issues.md creates debug sessions via `stclaude write-debug` in spawn_agents step and passes session_id to agents. debug-subagent-prompt.md template uses `stclaude write-debug --session-id` for updates. No .planning/debug/ file creation remains in either file.</done>
</task>

</tasks>

<verification>
1. `~/.claude/get-shit-done/workflows/add-todo.md` calls `stclaude add-todo` in create_file step; no gsd-tools.cjs call remains in data-path steps; no .planning/todos/ write remains
2. `~/.claude/get-shit-done/workflows/check-todos.md` calls `stclaude list-todos` in init_context; calls `stclaude complete-todo` in execute_action; no gsd-tools.cjs call in data-path steps; no mv .planning/todos/ command remains
3. `~/.claude/get-shit-done/workflows/diagnose-issues.md` creates debug sessions via `stclaude write-debug --bug` in spawn_agents; no .planning/debug/ directory creation remains
4. `~/.claude/get-shit-done/templates/debug-subagent-prompt.md` instructs agents to use `stclaude write-debug --session-id` for updates; no .planning/debug/ file write instruction remains
5. grep confirms: `grep -c "stclaude" ~/.claude/get-shit-done/workflows/add-todo.md` > 0
6. grep confirms: `grep -c "stclaude" ~/.claude/get-shit-done/workflows/check-todos.md` > 0
7. grep confirms: `grep -c "stclaude" ~/.claude/get-shit-done/workflows/diagnose-issues.md` > 0
</verification>

<success_criteria>
- add-todo GSD workflow captures todos in SpacetimeDB instead of .planning/todos/pending/*.md files
- check-todos GSD workflow lists todos from SpacetimeDB and marks completion via stclaude complete-todo
- diagnose-issues workflow creates debug sessions in SpacetimeDB and passes session IDs to debug subagents
- debug-subagent-prompt template updated to use stclaude write-debug --session-id for incremental updates
- All patched workflows: if stclaude is not configured, fail with clear setup instructions
</success_criteria>

<output>
After completion, store the summary via: stclaude write-summary (executor handles this, not planner)
</output>
