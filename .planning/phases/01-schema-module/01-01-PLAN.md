---
phase: 01-schema-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimeclaude/spacetimedb/src/schema.ts
autonomous: true
requirements:
  - SCHM-01
  - SCHM-02
  - SCHM-03
  - SCHM-04
  - SCHM-05
  - SCHM-06
  - SCHM-07
  - SCHM-08
  - SCHM-09
  - SCHM-10
  - SCHM-11
  - SCHM-12
  - SCHM-13
  - SCHM-15

must_haves:
  truths:
    - "All 13 tables are defined with correct column types in schema.ts"
    - "Foreign key columns use t.u64() with btree indexes for parent lookups"
    - "Index names are globally unique using {tableName}_{columnName} convention"
    - "Project table has git_remote_url as t.string().unique() for project identity"
    - "Prose fields use t.string() for raw markdown content"
    - "Plan summary table uses fully typed columns (not prose)"
    - "All tables are public: true"
    - "schema() export includes all 13 tables"
  artifacts:
    - path: "spacetimeclaude/spacetimedb/src/schema.ts"
      provides: "All table definitions and schema export"
      contains: "export default spacetimedb"
      min_lines: 150
  key_links:
    - from: "spacetimeclaude/spacetimedb/src/schema.ts"
      to: "spacetimedb/server"
      via: "import { schema, table, t }"
      pattern: "import.*schema.*table.*t.*from.*spacetimedb/server"
---

<objective>
Define all 13 SpacetimeDB table definitions in schema.ts

Purpose: Create the complete data model for GSD's core loop state -- project, phase, plan, task, requirement, project_state, continue_here, plan_summary, verification, research, phase_context, config, and must_have tables. This is the foundation that all reducers will operate on.

Output: `spacetimeclaude/spacetimedb/src/schema.ts` with all table definitions and `schema()` export.
</objective>

<execution_context>
@/Users/gustav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-module/01-CONTEXT.md
@.planning/phases/01-schema-module/01-RESEARCH.md
@spacetimeclaude/CLAUDE.md
@spacetimeclaude/spacetimedb/src/index.ts

<interfaces>
<!-- SpacetimeDB SDK v2.0.2 server-side API used in this plan -->

From spacetimedb/server:
```typescript
import { schema, table, t } from 'spacetimedb/server';

// table(OPTIONS, COLUMNS) -- indexes go in OPTIONS (1st arg), NOT columns
// t.u64().primaryKey().autoInc() -- auto-inc ID pattern
// t.string().unique() -- unique constraint (auto-indexed, do NOT add explicit index)
// t.string().optional() -- nullable field
// t.timestamp() -- native timestamp type
// schema({ table1, table2, ... }) -- single object argument, returns spacetimedb object
// Index: { name: '{tableName}_{columnName}', algorithm: 'btree', columns: ['columnName'] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schema.ts with core entity tables</name>
  <files>spacetimeclaude/spacetimedb/src/schema.ts</files>
  <action>
Create `spacetimeclaude/spacetimedb/src/schema.ts` with the following table definitions. Import `{ schema, table, t }` from `'spacetimedb/server'`.

**project table (SCHM-01, SCHM-15):**
- id: t.u64().primaryKey().autoInc()
- git_remote_url: t.string().unique() -- project identity, do NOT add explicit index (unique auto-indexes)
- name: t.string()
- description: t.string()
- core_value: t.string()
- constraints: t.string() -- prose markdown
- context: t.string() -- prose markdown
- key_decisions: t.string() -- prose markdown
- created_at: t.timestamp()
- updated_at: t.timestamp()
- public: true, no explicit indexes needed (PK + unique already indexed)

**phase table (SCHM-02):**
- id: t.u64().primaryKey().autoInc()
- project_id: t.u64()
- number: t.string() -- string for decimals like "2.1"
- name: t.string()
- slug: t.string()
- goal: t.string() -- prose markdown
- status: t.string()
- depends_on: t.string() -- JSON string of phase numbers
- success_criteria: t.string() -- prose markdown
- created_at: t.timestamp()
- updated_at: t.timestamp()
- public: true
- indexes: [{ name: 'phase_project_id', algorithm: 'btree', columns: ['project_id'] }]

**plan table (SCHM-03):**
- id: t.u64().primaryKey().autoInc()
- phase_id: t.u64()
- plan_number: t.u64()
- type: t.string() -- "execute" or "tdd"
- wave: t.u64()
- depends_on: t.string() -- JSON string of plan IDs
- objective: t.string() -- prose markdown
- autonomous: t.bool()
- requirements: t.string() -- JSON string of requirement IDs
- status: t.string()
- content: t.string() -- full plan markdown content
- created_at: t.timestamp()
- updated_at: t.timestamp()
- public: true
- indexes: [{ name: 'plan_phase_id', algorithm: 'btree', columns: ['phase_id'] }]

**plan_task table (SCHM-04):**
- id: t.u64().primaryKey().autoInc()
- plan_id: t.u64()
- task_number: t.u64()
- type: t.string() -- "auto", "checkpoint:human-verify", etc.
- description: t.string()
- status: t.string()
- commit_hash: t.string().optional()
- created_at: t.timestamp()
- updated_at: t.timestamp()
- public: true
- indexes: [{ name: 'plan_task_plan_id', algorithm: 'btree', columns: ['plan_id'] }]

**requirement table (SCHM-05):**
- id: t.u64().primaryKey().autoInc()
- project_id: t.u64()
- category: t.string() -- e.g., "SCHM", "CLI", "PTCH"
- number: t.string() -- e.g., "SCHM-01"
- description: t.string()
- status: t.string()
- phase_number: t.string() -- which phase covers this
- milestone_version: t.string().optional()
- created_at: t.timestamp()
- updated_at: t.timestamp()
- public: true
- indexes: [{ name: 'requirement_project_id', algorithm: 'btree', columns: ['project_id'] }]
  </action>
  <verify>
    <automated>cd /Users/gustav/src/spacetimeclaude/spacetimeclaude/spacetimedb && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>schema.ts exists with project, phase, plan, plan_task, and requirement table definitions. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add supporting tables and schema export</name>
  <files>spacetimeclaude/spacetimedb/src/schema.ts</files>
  <action>
Add the remaining 8 table definitions to schema.ts and add the `schema()` export at the bottom.

**project_state table (SCHM-06):**
- id: t.u64().primaryKey().autoInc()
- project_id: t.u64()
- current_phase: t.string()
- current_plan: t.u64()
- current_task: t.u64()
- last_activity: t.timestamp()
- last_activity_description: t.string()
- velocity_data: t.string() -- JSON string, agents parse client-side
- session_last: t.string() -- ISO date of last session
- session_stopped_at: t.string() -- what was happening when stopped
- session_resume_file: t.string().optional()
- updated_at: t.timestamp()
- public: true
- indexes: [{ name: 'project_state_project_id', algorithm: 'btree', columns: ['project_id'] }]

**continue_here table (SCHM-07):**
- id: t.u64().primaryKey().autoInc()
- project_id: t.u64()
- phase_id: t.u64()
- task_number: t.u64()
- current_state: t.string() -- prose markdown
- next_action: t.string() -- prose markdown
- context: t.string() -- prose markdown
- created_at: t.timestamp()
- updated_at: t.timestamp()
- public: true
- indexes: [{ name: 'continue_here_project_id', algorithm: 'btree', columns: ['project_id'] }]

**plan_summary table (SCHM-08):**
- id: t.u64().primaryKey().autoInc()
- plan_id: t.u64()
- subsystem: t.string()
- tags: t.string() -- JSON array string
- headline: t.string()
- accomplishments: t.string() -- JSON array string
- deviations: t.string() -- JSON array string
- files: t.string() -- JSON array string
- decisions: t.string() -- JSON array string
- dependency_graph: t.string() -- JSON string of graph metadata
- created_at: t.timestamp()
- updated_at: t.timestamp()
- public: true
- indexes: [{ name: 'plan_summary_plan_id', algorithm: 'btree', columns: ['plan_id'] }]

**verification table (SCHM-09):**
- id: t.u64().primaryKey().autoInc()
- phase_id: t.u64()
- status: t.string()
- score: t.u64()
- content: t.string() -- prose markdown
- recommended_fixes: t.string() -- prose markdown
- created_at: t.timestamp()
- updated_at: t.timestamp()
- public: true
- indexes: [{ name: 'verification_phase_id', algorithm: 'btree', columns: ['phase_id'] }]

**research table (SCHM-10):**
- id: t.u64().primaryKey().autoInc()
- phase_id: t.u64()
- domain: t.string()
- confidence: t.string()
- content: t.string() -- prose markdown
- created_at: t.timestamp()
- updated_at: t.timestamp()
- public: true
- indexes: [{ name: 'research_phase_id', algorithm: 'btree', columns: ['phase_id'] }]

**phase_context table (SCHM-11):**
- id: t.u64().primaryKey().autoInc()
- phase_id: t.u64()
- content: t.string() -- user decisions prose markdown
- created_at: t.timestamp()
- updated_at: t.timestamp()
- public: true
- indexes: [{ name: 'phase_context_phase_id', algorithm: 'btree', columns: ['phase_id'] }]

**config table (SCHM-12):**
- id: t.u64().primaryKey().autoInc()
- project_id: t.u64()
- config: t.string() -- JSON string
- created_at: t.timestamp()
- updated_at: t.timestamp()
- public: true
- indexes: [{ name: 'config_project_id', algorithm: 'btree', columns: ['project_id'] }]

**must_have table (SCHM-13):**
- id: t.u64().primaryKey().autoInc()
- plan_id: t.u64()
- truths: t.string() -- JSON array string
- artifacts: t.string() -- JSON array string
- key_links: t.string() -- JSON array string
- created_at: t.timestamp()
- updated_at: t.timestamp()
- public: true
- indexes: [{ name: 'must_have_plan_id', algorithm: 'btree', columns: ['plan_id'] }]

**After all tables, add the schema export:**
```typescript
const spacetimedb = schema({
  project,
  phase,
  plan,
  planTask,    // Note: table name 'plan_task' -> camelCase access
  requirement,
  projectState,
  continueHere,
  planSummary,
  verification,
  research,
  phaseContext,
  config,
  mustHave,
});

export default spacetimedb;
```

IMPORTANT: Use snake_case for table `name` properties (e.g., `name: 'plan_task'`). SpacetimeDB auto-converts to camelCase for `ctx.db` access. Variable names in the schema file should match what you pass to `schema()`.

Remove the old `index.ts` content that defines the `person` table and `schema()` -- that moves to `schema.ts` now.
  </action>
  <verify>
    <automated>cd /Users/gustav/src/spacetimeclaude/spacetimeclaude/spacetimedb && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>schema.ts has all 13 table definitions with correct types, indexes, and the schema() export. TypeScript compiles without errors. All tables are public: true. All foreign keys have btree indexes. All index names are globally unique with {tableName}_{columnName} convention.</done>
</task>

</tasks>

<verification>
1. `schema.ts` exists at `spacetimeclaude/spacetimedb/src/schema.ts`
2. File imports `{ schema, table, t }` from `'spacetimedb/server'`
3. 13 tables defined: project, phase, plan, planTask, requirement, projectState, continueHere, planSummary, verification, research, phaseContext, config, mustHave
4. `schema()` called with all 13 tables, `export default spacetimedb`
5. All auto-inc IDs use `t.u64().primaryKey().autoInc()`
6. `git_remote_url` uses `t.string().unique()` with NO explicit index
7. All timestamp columns use `t.timestamp()`
8. All prose content fields use `t.string()`
9. Plan summary has individual typed columns (subsystem, tags, headline, etc.)
10. No index on `.primaryKey()` or `.unique()` columns
11. TypeScript compiles: `npx tsc --noEmit` passes
</verification>

<success_criteria>
- schema.ts defines 13 tables covering SCHM-01 through SCHM-13 and SCHM-15
- All index names are globally unique
- TypeScript type-checks successfully
- No anti-patterns from CLAUDE.md (indexes in columns, index on PK/unique, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-module/01-01-SUMMARY.md`
</output>
