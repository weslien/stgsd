---
phase: 15-patch-verification-tooling
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - /Users/gustav/.claude/commands/stclaude/verify-patches.md
  - /Users/gustav/src/spacetimeclaude/package.json
  - /Users/gustav/.claude/get-shit-done/workflows/update.md
autonomous: true
requirements: []

must_haves:
  truths:
    - /stclaude:verify-patches slash command exists at ~/.claude/commands/stclaude/verify-patches.md
    - Slash command runs ~/.claude/bin/stclaude verify-patches and reports output
    - Slash command suggests /gsd:reapply-patches on failures
    - install:cli script in package.json copies patch-manifest.json to ~/.claude/stclaude/
    - update.md workflow includes a step to run stclaude verify-patches after reapply-patches
    - The verify step in update.md runs AFTER the reapply-patches step, not before
    - stclaude verify-patches works end-to-end after npm run install:cli
  artifacts:
    - /Users/gustav/.claude/commands/stclaude/verify-patches.md
    - /Users/gustav/src/spacetimeclaude/package.json
    - /Users/gustav/.claude/get-shit-done/workflows/update.md
  key_links:
    - Slash command wraps the CLI command built in plan 15-02
    - Install script copies the manifest created in plan 15-01
    - Update workflow integration triggers verify after GSD updates
---

<objective>
Wire up the verify-patches command with a slash command, install script, and update workflow integration.

Purpose: Make verification accessible via `/stclaude:verify-patches` in Claude Code, ensure the manifest is installed alongside the CLI, and auto-verify after GSD updates.

Output: Slash command file, updated install script, patched update workflow.
</objective>

<execution_context>
@/Users/gustav/.claude/commands/stclaude/status.md
@/Users/gustav/src/spacetimeclaude/.claude/worktrees/phase-10/package.json
</execution_context>

<context>
Three integration points need wiring:

1. **Slash command** at `~/.claude/commands/stclaude/verify-patches.md` — follows the exact pattern of existing stclaude slash commands (see status.md for reference)

2. **Install script** in `package.json` `install:cli` — add `cp patch-manifest.json ~/.claude/stclaude/patch-manifest.json` after the existing `cp` commands. The manifest must be available at `~/.claude/stclaude/patch-manifest.json` at runtime.

3. **Update workflow** at `~/.claude/get-shit-done/workflows/update.md` — after the `check_local_patches` step that suggests `/gsd:reapply-patches`, add a verification step that runs `stclaude verify-patches` to confirm patches are correctly applied. This step should:
   - Run AFTER reapply-patches (not before)
   - Show the verification results
   - If failures exist, remind user to run `/gsd:reapply-patches`
   - Be a suggestion, not a blocker (the update itself already succeeded)

**Current install:cli command** (from package.json):
```
bun run build:cli && mkdir -p ~/.claude/bin && cp dist/stclaude.mjs ~/.claude/bin/stclaude.mjs && chmod +x ~/.claude/bin/stclaude.mjs && ln -sf stclaude.mjs ~/.claude/bin/stclaude && mkdir -p ~/.local/bin && ln -sf ~/.claude/bin/stclaude.mjs ~/.local/bin/stclaude && mkdir -p ~/.claude/stclaude/module && cp -r spacetimedb/src spacetimedb/package.json spacetimedb/tsconfig.json ~/.claude/stclaude/module/ && (cd ~/.claude/stclaude/module && npm install 2>/dev/null || bun install 2>/dev/null || true)
```

Add `cp patch-manifest.json ~/.claude/stclaude/patch-manifest.json` after the `chmod +x` line (before the symlink to ~/.local/bin).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create slash command and update install script</name>
  <files>
    /Users/gustav/.claude/commands/stclaude/verify-patches.md
    /Users/gustav/src/spacetimeclaude/package.json
  </files>
  <action>
1. Create `~/.claude/commands/stclaude/verify-patches.md`:

```markdown
---
name: stclaude:verify-patches
description: Verify all stclaude patches are correctly applied to GSD files
allowed-tools:
  - Bash
---
Run the stclaude verify-patches command:

\`\`\`bash
~/.claude/bin/stclaude verify-patches
\`\`\`

Report the output to the user.

If any failures are reported, suggest running `/gsd:reapply-patches` to restore missing patches.
```

2. Edit `package.json` to add manifest copy to the `install:cli` script. Insert `cp patch-manifest.json ~/.claude/stclaude/patch-manifest.json &&` into the install:cli chain, after `chmod +x ~/.claude/bin/stclaude.mjs &&` and before `ln -sf stclaude.mjs ~/.claude/bin/stclaude`.

3. Run `npm run install:cli` to verify the build and install works (this also copies the manifest).

4. Run `stclaude verify-patches` to confirm end-to-end functionality. If the manifest from plan 15-01 is already in place, this should produce actual verification results. If not yet available, confirm the command runs and reports MANIFEST_NOT_FOUND gracefully.
  </action>
  <verify>
    <automated>test -f ~/.claude/commands/stclaude/verify-patches.md && grep -c "verify-patches" ~/.claude/commands/stclaude/verify-patches.md && grep -c "patch-manifest.json" /Users/gustav/src/spacetimeclaude/package.json</automated>
  </verify>
  <done>Slash command file exists with correct format. package.json install:cli copies patch-manifest.json to ~/.claude/stclaude/.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate verify step into GSD update workflow</name>
  <files>/Users/gustav/.claude/get-shit-done/workflows/update.md</files>
  <action>
Read `~/.claude/get-shit-done/workflows/update.md`. Find the `check_local_patches` step (the last step in the workflow). After it, add a new step:

```markdown
<step name="verify_patches">
After update and any patch reapplication, verify stclaude patches are intact:

```bash
stclaude verify-patches 2>/dev/null
```

**If stclaude is not installed** (command not found): Skip silently — stclaude verification is optional.

**If verification passes:**
```
All stclaude patches verified.
```

**If verification fails:**
Show the verification output and suggest:
```
Some stclaude patches may need reapplication.
Run /gsd:reapply-patches to restore them.
```

**Note:** This step is informational only — it does not block the update.
</step>
```

Place this step AFTER `check_local_patches` and BEFORE the `</process>` closing tag. Also add a verification checkbox to the `<success_criteria>` section: `- [ ] stclaude patches verified (if stclaude installed)`.
  </action>
  <verify>
    <automated>grep -c "verify_patches\|verify-patches" ~/.claude/get-shit-done/workflows/update.md</automated>
  </verify>
  <done>update.md contains a verify_patches step that runs stclaude verify-patches after the check_local_patches step. The step is informational and does not block updates.</done>
</task>

</tasks>

<verification>
1. `test -f ~/.claude/commands/stclaude/verify-patches.md` — slash command exists
2. `grep "patch-manifest.json" /Users/gustav/src/spacetimeclaude/package.json` — install script copies manifest
3. `test -f ~/.claude/stclaude/patch-manifest.json` — manifest is installed (after running install:cli)
4. `grep "verify_patches" ~/.claude/get-shit-done/workflows/update.md` — update workflow has verify step
5. `stclaude verify-patches --json 2>/dev/null` — command runs end-to-end (may fail if manifest not yet built)
</verification>

<success_criteria>
- /stclaude:verify-patches slash command exists and follows existing format
- install:cli copies patch-manifest.json to ~/.claude/stclaude/
- GSD update workflow runs verify-patches after reapply-patches step
- Verify step is informational, not blocking
- End-to-end: after install:cli, `stclaude verify-patches` runs successfully
</success_criteria>
