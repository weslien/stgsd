---
phase: 07-schema-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/schema.ts
  - spacetimedb/src/index.ts
autonomous: true
requirements:
  - MILE-01
  - MILE-02
  - MILE-03
  - SESS-01
  - PHSE-01
  - PHSE-02
  - TODO-01
  - DBG-01
  - CMAP-01
must_haves:
  truths:
    - "SpacetimeDB module publishes with 0 schema errors containing all 19 tables (13 v1.0 + 6 new)"
    - "Phase table includes is_inserted boolean column"
    - "seed_project handles is_inserted field with default false"
    - "delete_project cascades to all 6 new tables (milestone, milestone_audit, session_checkpoint, todo, debug_session, codebase_map)"
    - "delete_phase cascades to session_checkpoint records"
    - "All new tables have correct btree indexes for project_id and status/type lookups"
    - "stgsd list-projects returns existing projects after clear-database republish and re-seed"
  artifacts:
    - path: "spacetimedb/src/schema.ts"
      provides: "All 19 table definitions with indexes"
      contains: "milestone, milestoneAudit, sessionCheckpoint, todo, debugSession, codebases"
    - path: "spacetimedb/src/index.ts"
      provides: "All reducers for new tables plus updated cascades"
      contains: "insert_milestone, upsert_session_checkpoint, insert_todo, insert_debug_session, upsert_codebase_map"
  key_links:
    - from: "spacetimedb/src/schema.ts"
      to: "spacetimedb/src/index.ts"
      via: "default export spacetimedb"
      pattern: "import spacetimedb from './schema'"
    - from: "spacetimedb/src/index.ts delete_project"
      to: "all new tables"
      via: "cascade delete pattern"
      pattern: "ctx\\.db\\.(milestone|todo|debugSession|codebases|sessionCheckpoint)"
    - from: "spacetimedb/src/index.ts delete_phase"
      to: "session_checkpoint"
      via: "cascade delete"
      pattern: "ctx\\.db\\.sessionCheckpoint\\.session_checkpoint_phase_id\\.filter"
---

<objective>
Verify and validate that all v1.1 schema extensions (6 new tables, phase table extension, reducers, cascades) are correctly implemented and the module publishes successfully.

Purpose: Phase 7 requires all v1.1 data structures to exist in SpacetimeDB. The schema and reducers were implemented during roadmap planning. This plan verifies correctness by publishing the module, re-seeding, and confirming no regressions.

Output: Published SpacetimeDB module with all 19 tables, verified via publish + re-seed + list-projects round-trip.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@spacetimedb/src/schema.ts
@spacetimedb/src/index.ts

<interfaces>
<!-- The schema and reducers are already fully implemented. Executor verifies correctness. -->

From spacetimedb/src/schema.ts (19 tables):
- project, phase (with is_inserted: t.bool()), plan, planTask, requirement
- projectState, continueHere, planSummary, verification, research, phaseContext, config, mustHave
- milestone (NEW: project_id indexed, status indexed)
- milestoneAudit (NEW: project_id, milestone_id, audit_status indexed)
- sessionCheckpoint (NEW: project_id, phase_id indexed)
- todo (NEW: project_id, status, area indexed)
- debugSession (NEW: project_id, status indexed)
- codebases (NEW: project_id, doc_type indexed)

From spacetimedb/src/index.ts (new reducers):
- insert_milestone, update_milestone, delete_milestone (cascades to audits)
- insert_milestone_audit, update_milestone_audit, delete_milestone_audit
- upsert_session_checkpoint, delete_session_checkpoint
- insert_todo, update_todo, delete_todo
- insert_debug_session, update_debug_session, delete_debug_session
- upsert_codebase_map, delete_codebase_map
- delete_project: cascades to milestones, todos, debug_sessions, codebase_maps, session_checkpoints
- delete_phase: cascades to session_checkpoints
- seed_project: handles is_inserted field (defaults to false)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit schema and reducers for completeness against requirements</name>
  <files>spacetimedb/src/schema.ts, spacetimedb/src/index.ts</files>
  <action>
Read schema.ts and index.ts. Verify every requirement is covered:

1. **MILE-01** (milestone table): Confirm milestone table has project_id, version, name, shipped_date, phase_count, plan_count, requirement_count, accomplishments, status, timestamps. Confirm milestone_project_id and milestone_status indexes. Confirm insert/update/delete reducers.

2. **MILE-02** (milestone_audit table): Confirm milestone_audit has project_id, milestone_id, audit_status, requirement_scores, integration_scores, flow_scores, tech_debt_items, timestamps. Confirm milestone_audit_project_id, milestone_audit_milestone_id, milestone_audit_status indexes. Confirm insert/update/delete reducers.

3. **MILE-03** (archive fields): Confirm milestone_audit has roadmap_content and requirements_content string columns.

4. **SESS-01** (session_checkpoint table): Confirm session_checkpoint has project_id, phase_id, phase_context, completed_work, remaining_work, decisions, blockers, next_action, mental_context, timestamps. Confirm session_checkpoint_project_id and session_checkpoint_phase_id indexes. Confirm upsert reducer (filters by phase_id, updates if exists, inserts if not).

5. **PHSE-01** (decimal numbering): Confirm phase.number is t.string() (already supports decimals like "3.1"). No schema change needed.

6. **PHSE-02** (is_inserted flag): Confirm phase table has `is_inserted: t.bool()`. Confirm insert_phase and update_phase accept is_inserted parameter. Confirm seed_project handles is_inserted with `p.is_inserted ?? false` default.

7. **TODO-01** (todo table): Confirm todo has project_id, title, area, problem, solution_hints, file_refs, status, timestamps. Confirm todo_project_id, todo_status, todo_area indexes. Confirm insert/update/delete reducers.

8. **DBG-01** (debug_session table): Confirm debug_session has project_id, bug_description, hypotheses, checkpoints, timeline, status, resolution_notes (optional), timestamps. Confirm debug_session_project_id and debug_session_status indexes. Confirm insert/update/delete reducers.

9. **CMAP-01** (codebase_map table): Confirm codebases table has project_id, doc_type, content, timestamps. Confirm codebase_map_project_id and codebase_map_doc_type indexes. Confirm upsert reducer (filters by project_id, finds by doc_type in memory).

10. **Cascade correctness**: Confirm delete_project cascades to milestones (then audits), todos, debug_sessions, codebase_maps. Confirm delete_phase cascades to session_checkpoints.

11. **Schema export**: Confirm all 19 tables are in the `schema({...})` call.

If any requirement is missing or incorrect, fix it. If all pass, document the audit results.
  </action>
  <verify>
    <automated>cd C:/Development/stgsd/spacetimedb && cat src/schema.ts | grep -c "table(" && cat src/index.ts | grep -c "spacetimedb.reducer("</automated>
  </verify>
  <done>All 9 requirements (MILE-01/02/03, SESS-01, PHSE-01/02, TODO-01, DBG-01, CMAP-01) verified present with correct columns, indexes, reducers, and cascades. Any gaps fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Publish module and verify round-trip with re-seed</name>
  <files>spacetimedb/src/schema.ts, spacetimedb/src/index.ts</files>
  <action>
1. Ensure SpacetimeDB local server is running: `spacetime start` (if not already running).

2. Publish the module with `--clear-database` (or `--delete-data=always`):
   ```
   spacetime publish stgsd --delete-data=always -y --module-path spacetimedb --server local --no-config
   ```
   This validates the schema compiles and deploys with all 19 tables. If publish fails, diagnose and fix the schema/reducer error.

3. After publish succeeds, run `stgsd seed --force` to re-seed the project data:
   ```
   stgsd seed --force --name "stgsd" --description "SpacetimeDB-backed GSD planning state" --core-value "GSD planning state as structured queryable data" --phases-json "[...]" --requirements-json "[...]"
   ```
   Use the existing seed skill/command pattern. The phases JSON must include `is_inserted: false` for all phases.

4. After seed, verify the project exists:
   ```
   stgsd list-projects --json
   ```
   Confirm it returns the project with correct name and git remote URL.

5. Verify no schema errors in logs:
   ```
   spacetime logs stgsd
   ```

If any step fails, fix the issue and retry. The goal is a clean publish + seed + query round-trip proving all v1.1 schema extensions are operational.
  </action>
  <verify>
    <automated>cd C:/Development/stgsd && npx stgsd list-projects --json 2>/dev/null || node dist/cli/index.js list-projects --json</automated>
  </verify>
  <done>Module publishes with 0 errors. Re-seed succeeds. list-projects returns the project. All 19 tables exist in SpacetimeDB. No regressions from v1.0.</done>
</task>

</tasks>

<verification>
1. `spacetime publish` completes with 0 errors for the module containing all 19 tables
2. `stgsd list-projects` returns at least one project after re-seed
3. All 6 new tables (milestone, milestone_audit, session_checkpoint, todo, debug_session, codebase_map) are present in published module
4. Phase table includes is_inserted column
5. seed_project handles is_inserted with default false
</verification>

<success_criteria>
- SpacetimeDB module publishes with 19 tables and 0 schema errors
- Re-seed completes without type errors (is_inserted handled correctly)
- list-projects confirms v1.0 compatibility (project data survives clear + re-seed)
- All new table indexes are correctly defined (btree on project_id, status/type columns)
- Cascade deletes cover all new tables in both delete_project and delete_phase
</success_criteria>

<output>
After completion, create `.planning/phases/07-schema-extensions/07-01-SUMMARY.md`
</output>
