---
phase: 12-workflow-patch-completion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/gustav/.claude/get-shit-done/workflows/verify-phase.md
  - /Users/gustav/.claude/get-shit-done/workflows/transition.md
autonomous: true
requirements: []

must_haves:
  truths:
    - verify-phase.md no longer calls gsd-tools init phase-op or gsd-tools roadmap get-phase
    - verify-phase.md reads plan must_haves from stclaude init execute-phase JSON, not disk PLAN.md frontmatter via gsd-tools
    - verify-phase.md no longer writes VERIFICATION.md to disk; return_to_orchestrator returns status inline
    - transition.md no longer reads STATE.md, PROJECT.md, ROADMAP.md from disk
    - transition.md replaces gsd-tools phase complete with stclaude complete-phase
    - transition.md replaces STATE.md direct writes with stclaude commands
    - transition.md checks phase completion using stclaude init execute-phase JSON plan counts
  artifacts:
    - /Users/gustav/.claude/get-shit-done/workflows/verify-phase.md
    - /Users/gustav/.claude/get-shit-done/workflows/transition.md
  key_links:
    - verify-phase.md establish_must_haves reads INIT.plans[].mustHaves from stclaude JSON
    - transition.md load_project_state uses stclaude get-state --json
    - transition.md complete-phase call uses stclaude complete-phase
---

<objective>
Patch verify-phase.md and transition.md to replace disk I/O and gsd-tools commands with stclaude CLI equivalents.

Purpose: verify-phase.md (the verification orchestrator) still calls gsd-tools init phase-op, reads PLAN.md frontmatter from disk, and writes VERIFICATION.md to disk. transition.md reads STATE.md, PROJECT.md, ROADMAP.md from disk and calls gsd-tools phase complete. Both must be updated.

Output: Both workflows patched to use stclaude for state reading, plan data, and phase completion.
</objective>

<execution_context>
@/Users/gustav/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
Key notes for verify-phase.md:
- gsd-verifier.md (the spawned subagent) is ALREADY patched — it uses stclaude write-verification and stclaude commands. Only verify-phase.md (the orchestrator) needs patching.
- The `establish_must_haves` step (Option A) uses `gsd-tools frontmatter get "$plan" --field must_haves`. With stclaude, must_haves come from `INIT.plans[].mustHaves` in the execute-phase JSON.
- The `verify_artifacts` and `verify_wiring` steps use `gsd-tools verify artifacts "$plan"` and `gsd-tools verify key-links "$plan"` — these operate on disk PLAN.md files. These verification checks may remain disk-based IF PLAN.md files still exist on disk, OR delegate to the gsd-verifier subagent which handles this.
- The `create_report` step writes VERIFICATION.md to `$PHASE_DIR/`. Since gsd-verifier.md already calls `stclaude write-verification`, the orchestrator's file-write can be removed.

Key notes for transition.md:
- PROJECT.md and ROADMAP.md remain disk files (not yet in stclaude v1). Keep PROJECT.md updates as-is.
- `complete-phase` in stclaude handles roadmap/state advancement — this replaces `gsd-tools phase complete`.
- STATE.md direct section updates (blockers, decisions, session) should use stclaude commands where available.
- The `ls .planning/phases/XX-current/*-PLAN.md` and `*-SUMMARY.md` disk listing should use `stclaude init execute-phase` JSON (plans[].hasSummary) for completion checking.

<interfaces>
From stclaude CLI (verified):
- stclaude init execute-phase <phase> --json
  Returns: { phase, plans[{id,planNumber,status,content,tasks[],hasSummary,mustHaves[]}], requirements[] }
- stclaude complete-phase <phase> --json
  Marks phase complete, advances state
- stclaude get-state --json
  Returns: { project, state{currentPhase,currentPlan,lastActivity,...}, phases[], plans[] }
- stclaude roadmap analyze --json
  Returns: { phases[], milestones[], requirements[] }
- stclaude write-verification --phase <N> --status <s> --score <n> --content <text> [--recommended-fixes <yaml>] --json
  (Already used by gsd-verifier.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch verify-phase.md orchestrator</name>
  <files>/Users/gustav/.claude/get-shit-done/workflows/verify-phase.md</files>
  <action>
Read the full verify-phase.md file. Replace all legacy gsd-tools and .planning/ disk I/O patterns:

1. **load_context step (priority="first")**: Replace:
   ```bash
   INIT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init phase-op "${PHASE_ARG}")
   ```
   With:
   ```bash
   INIT=$(stclaude init execute-phase "${PHASE_ARG}" --json)
   ```
   Remove extraction of `phase_dir` (disk path). Extract from JSON: `phase_number = INIT.phase.number`, `phase_name = INIT.phase.name`, `plans = INIT.plans`. Remove `ls "$phase_dir"/*-SUMMARY.md` and `ls "$phase_dir"/*-PLAN.md` disk listing — use `INIT.plans` instead. Replace `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap get-phase "${phase_number}"` with reading `INIT.phase.goal` (goal is in init JSON). Remove `grep -E "^| ${phase_number}" .planning/REQUIREMENTS.md` — use `INIT.requirements` instead.

2. **establish_must_haves step (Option A)**: Replace:
   ```bash
   for plan in "$PHASE_DIR"/*-PLAN.md; do
     MUST_HAVES=$(node ... frontmatter get "$plan" --field must_haves)
   done
   ```
   With: iterate over `INIT.plans` and extract `plan.mustHaves` from the JSON array. Each plan's mustHaves object contains `{truths:[], artifacts:[], key_links:[]}`. Aggregate across all plans for phase-level verification.

   **Option B**: Replace `node ... roadmap get-phase "${phase_number}" --raw` with reading `INIT.phase.goal` and `INIT.phase.successCriteria` from JSON.

3. **verify_artifacts step**: The `gsd-tools verify artifacts "$plan"` command checks disk PLAN.md frontmatter. Since this orchestrator now delegates to gsd-verifier.md subagent, AND gsd-verifier.md is already patched to use stclaude:
   - Simplify: remove the disk-based `for plan in "$PHASE_DIR"/*-PLAN.md` loop for artifact verification
   - The spawned gsd-verifier subagent handles artifact/wiring verification directly

4. **verify_wiring step**: Same as above — remove disk-based `gsd-tools verify key-links "$plan"` loop. Delegate to gsd-verifier.

5. **verify_requirements step**: Replace `grep -E "Phase ${PHASE_NUM}" .planning/REQUIREMENTS.md` with `INIT.requirements` array from step 1.

6. **create_report step**: Remove `REPORT_PATH="$PHASE_DIR/${PHASE_NUM}-VERIFICATION.md"` and the file write. The verification report is persisted by the gsd-verifier subagent via `stclaude write-verification`. The orchestrator returns status inline.

7. **return_to_orchestrator step**: Update to return status based on verification results in memory (not a file path). Remove any reference to returning a disk file path.

Keep the `spawn gsd-verifier subagent` logic intact — just ensure the spawned prompt passes phase data from INIT JSON (not file paths).
  </action>
  <verify>
    <automated>grep -c "gsd-tools.cjs" /Users/gustav/.claude/get-shit-done/workflows/verify-phase.md</automated>
  </verify>
  <done>verify-phase.md contains 0 references to gsd-tools.cjs. load_context uses stclaude init execute-phase. establish_must_haves reads from INIT.plans[].mustHaves. verify_requirements uses INIT.requirements. No disk VERIFICATION.md write in orchestrator.</done>
</task>

<task type="auto">
  <name>Task 2: Patch transition.md</name>
  <files>/Users/gustav/.claude/get-shit-done/workflows/transition.md</files>
  <action>
Read the full transition.md file. Replace all legacy gsd-tools and .planning/ disk I/O patterns:

1. **required_reading section**: Remove the instructions to read STATE.md, PROJECT.md, ROADMAP.md, and plan files from disk. Replace with:
   ```
   Load project state via stclaude before any operation.
   ```

2. **load_project_state step (priority="first")**: Replace:
   ```bash
   cat .planning/STATE.md 2>/dev/null
   cat .planning/PROJECT.md 2>/dev/null
   ```
   With:
   ```bash
   STATE=$(stclaude get-state --json)
   ROADMAP=$(stclaude roadmap analyze --json)
   ```
   Extract current phase from `STATE.state.currentPhase`. Note: PROJECT.md still lives on disk — keep `cat .planning/PROJECT.md` if the workflow needs PROJECT.md content. Just remove STATE.md read.

3. **verify_completion step**: Replace:
   ```bash
   ls .planning/phases/XX-current/*-PLAN.md 2>/dev/null | sort
   ls .planning/phases/XX-current/*-SUMMARY.md 2>/dev/null | sort
   ```
   With:
   ```bash
   PHASE_INIT=$(stclaude init execute-phase "${current_phase}" --json)
   ```
   Count plans: `PHASE_INIT.plans.length`. Count completed plans: `PHASE_INIT.plans.filter(p => p.hasSummary).length`. If counts match → all plans complete.

   Remove `cat .planning/config.json` — config is embedded in state.

   Also remove `ls .planning/phases/XX-current/.continue-here*.md` disk check (obsolete).

4. **cleanup_handoff step** (or equivalent phase completion step): Replace:
   ```bash
   node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" phase complete "${current_phase}"
   ```
   With:
   ```bash
   stclaude complete-phase "${current_phase}" --json
   ```

5. **Read SUMMARY files**: Replace `cat .planning/phases/XX-current/*-SUMMARY.md` with using `PHASE_INIT.plans[].content` or `hasSummary` status — SUMMARY content may need to stay disk-based if SUMMARY.md files still exist (they're written by executors). Keep SUMMARY file reads via disk if content is needed for transition summary.

6. **STATE.md direct update sections**: Remove all sections that directly write to STATE.md sections (blockers, decisions, session). Replace with stclaude equivalents:
   - Session tracking: `stclaude write-session --json` if command exists
   - Progress update: `stclaude update-progress --json`
   - If stclaude commands don't exist for a specific update: remove the update step (SpacetimeDB tracks state via complete-phase)

7. **gsd-tools progress bar**: Replace `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" progress bar --raw` with `stclaude roadmap analyze --json` (phases with status information).

8. **Keep PROJECT.md on disk**: PROJECT.md updates (requirements validated/invalidated section) remain as disk writes — PROJECT.md is not yet in stclaude v1.

After patching: verify the transition flow works — load state from stclaude → check completion via execute-phase JSON → call stclaude complete-phase → update PROJECT.md (disk) if needed.
  </action>
  <verify>
    <automated>grep -c "gsd-tools.cjs" /Users/gustav/.claude/get-shit-done/workflows/transition.md</automated>
  </verify>
  <done>transition.md contains 0 references to gsd-tools.cjs. load_project_state uses stclaude get-state. verify_completion uses stclaude init execute-phase JSON for plan counts. Phase completion calls stclaude complete-phase. STATE.md direct writes replaced with stclaude commands or removed.</done>
</task>

</tasks>

<verification>
After completing both tasks:
1. Run: `grep -rn "gsd-tools.cjs" /Users/gustav/.claude/get-shit-done/workflows/verify-phase.md /Users/gustav/.claude/get-shit-done/workflows/transition.md` — should return 0 matches
2. Run: `grep -n "phase complete" /Users/gustav/.claude/get-shit-done/workflows/transition.md` — should show `stclaude complete-phase`, not `gsd-tools phase complete`
3. Run: `grep -n "cat .planning/STATE" /Users/gustav/.claude/get-shit-done/workflows/transition.md` — should return 0 matches
4. Run: `grep -n "frontmatter get" /Users/gustav/.claude/get-shit-done/workflows/verify-phase.md` — should return 0 matches
5. Run: `grep -n "stclaude init execute-phase" /Users/gustav/.claude/get-shit-done/workflows/verify-phase.md` — should show the stclaude init call
</verification>

<success_criteria>
- verify-phase.md has 0 gsd-tools.cjs references
- verify-phase.md reads must_haves from stclaude init execute-phase JSON plan objects
- transition.md has 0 gsd-tools.cjs references
- transition.md calls stclaude complete-phase for phase completion
- transition.md reads state from stclaude get-state --json
- transition.md checks plan completion via stclaude init execute-phase JSON hasSummary flags
</success_criteria>

<output>
After completion, store the summary via: stclaude write-summary (executor handles this, not planner)
</output>
